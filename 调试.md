#查看内存占用
ps -eo pid,user,%mem,vsz,rss,cmd --sort=-rss | head -n 11


#打印相关子进程：
ps -ef --forest | grep -E "790|fmssl_proxy_pass"


#查看内存映射表
pmap -x 794 | grep "anon" | sort -n -k 2

watch -n 1 "pmap -x 2678"
watch -n 1 "pmap -x 23497| grep "anon" | sort -n -k 2"

Address (0000000000bff000)：虚拟内存起始地址。这个值没变，说明两次看的绝对是同一个核心内存池映射区。Kbytes (4580236 $\rightarrow$ 4156712)：虚拟内存大小。代表进程向内核“画大饼”申请的地址空间总量。第一次申请了约 4.36 GB；第二次申请了约 3.96 GB。RSS (1248604 $\rightarrow$ 1133120)：物理驻留内存。代表当前真正吃掉并固化在物理内存条上的空间（真正花了多少钱）。第一次实际占了 1.19 GB；第二次实际占了 1.08 GB。Dirty (1248604 $\rightarrow$ 1133120)：脏页大小。代表被网关流数据实质性修改、写入过的物理内存。这里它依然和 RSS 保持百分之百完全相等，说明里面的数据全被写满了。Mode (rw---)：访问权限。rw 代表可读写，--- 代表严禁执行代码（纯数据安全区）。Mapping ([ anon ])：映射类型。匿名映射，不绑定磁盘文件，纯粹由 C 代码动态向内核索要的内存大平层。

    Address          Kbytes   RSS    Dirty        Mapping
00007fcf8fa4f000       4       4       4 rw---   [ anon ]
00007fcf8fe82000       4       4       4 rw---   [ anon ]
ffffffffff600000       4       0       0 r-x--   [ anon ]
000000000060b000       8       4       4 rw---   [ anon ]
00007fcf8fe7e000       8       8       8 rw---   [ anon ]
00007ffc5ad8f000       8       4       0 r-x--   [ anon ]
00007ffc5ad8c000      12       0       0 r----   [ anon ]
00007fcf8f7ec000      16       4       4 rw---   [ anon ]
00007fcf8fe74000      16      16      16 rw---   [ anon ]
00007fcf8f5cf000      20      12      12 rw---   [ anon ]
00007fcf8f1ec000     104      12      12 rw---   [ anon ]
0000000000bde000     132      92      92 rw---   [ anon ]
0000000000bff000 4157500 1133340 1133340 rw---   [ anon ]


kswapd0 物理内存紧急回收，显示内存，不够了用swap在不够了用内存 然后kswapd0 杀掉高内存占用进程 
卡死也就两三分钟，但是确实可以,但如果cpu分片分不到，其实oom killer触发不了的



journalctl -b -1 -k | grep -i -E "Out of memory:"

journalctl -b -1 -k --since "2026-05-08 10:50:00" --until "2026-05-08 10:55:00"

#查看上次关机时间
last -x|grep shutdown | head -1

#开关机及持续时间
last -x | grep -E "reboot|shutdown" | head -n 30

shutdown system down  4.14.89          Sat May  9 11:23 - 13:59  (02:35)


grep -rE "May  9 11:|May 09 11:" /var/log/ --exclude=*.gz 2>/dev/null | tail -n 100

cat /proc/swaps 或 lsblk 查看swap分区在哪里


df -lh /var/log
iftop -i 网卡 吞吐
top -p pid  cpu RSDZ TIME+越少越好？？ 底下的最多是核心数目*100%
pmap -x pid 内存
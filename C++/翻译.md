1、直接安装Python环境 https://www.python.org/downloads/release/python-3119/
2、git clone https://github.com/ifocus9/PopTrans.git
3、
python -m venv my_env
.\my_env\Scripts\Activate.ps1
5、若需要则
# 1. 直接用命令修改注册表，解锁系统长路径限制
Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' -Name 'LongPathsEnabled' -Value 1
6、
141行改代码
from huggingface_hub import hf_hub_download

# 尝试导入旧版本的配置函数
try:
    from huggingface_hub import configure_http_backend
except ImportError:
    # 如果是新版 huggingface_hub（用 httpx 的版本），就定义一个空函数让它安全跳过
    def configure_http_backend(*args, **kwargs):
        pass
7、需要关闭代理服务 源码内用的国内地址
8、按照github指示操作
9、实测约等于谷歌在线翻译效果


性能：

-O2是优化
-g是调试
两个是独立的互不影响的
实际最后可以使用strip给去掉 debug_info 和 not stripped

makefile 交叉编译：


CROSS_COMPILE = 
ifneq (,$(findstring zig,$(CROSS_COMPILE)))
  # zig cc 模式 — CROSS_COMPILE 是完整的 CC 命令
  CC       = $(CROSS_COMPILE)
  CXX      = $(subst cc,c++,$(CC))
  LD       = $(CC)
  ZIG_CMD  = $(firstword $(CROSS_COMPILE))
  AR       = $(ZIG_CMD) ar
  OBJCOPY  = $(ZIG_CMD) objcopy
  OBJDUMP  = $(ZIG_CMD) objdump
  # zig 没有 nm/readelf/strip/size，回退到 host 工具
  # 注意 host strip 无法处理异构 ELF，如需 strip 建议链接时加 -Wl,-s
  NM       = nm
  READELF  = readelf
  STRIP    = strip
  OBJSIZE  = size
else
  # 标准 GNU 交叉编译链 (如 aarch64-linux-gnu-)
  CC       = $(CROSS_COMPILE)gcc
  CXX      = $(CROSS_COMPILE)g++
  LD       = $(CROSS_COMPILE)gcc
  AR       = $(CROSS_COMPILE)ar
  NM       = $(CROSS_COMPILE)nm
  OBJCOPY  = $(CROSS_COMPILE)objcopy
  OBJDUMP  = $(CROSS_COMPILE)objdump
  READELF  = $(CROSS_COMPILE)readelf
  STRIP    = $(CROSS_COMPILE)strip
  OBJSIZE  = $(CROSS_COMPILE)size
endif

CFLAGS  :=   -O2 -fPIC

LDFLAGS = -shared -Wl,-rpath='$$ORIGIN' -Wl,--version-script=$(ROOT)/export.map

export CFLAGS LDFLAGS
export CROSS_COMPILE CC LD CXX AR NM OBJCOPY OBJDUMP READELF OBJSIZE STRIP



脚本：


#!/bin/bash
set -e

ZIG=${ZIG:-zig}
PROJECT_ROOT=$(pwd)

# 最终输出的绝对路径
OUTS=(
    "/home/b/github/fisec/lib/x86"
    "/home/b/github/fisec/lib/mips"
    "/home/b/github/fisec/lib/arm"
    "/home/b/github/fisec/lib/loongarch"
)

# 架构简称（用于 PQC 工程定位其 release_linux/<arch> 目录）
ARCHS=(
    "x86"
    "mips"
    "arm"
    "loongarch"
)

# 修正：将点号 '.' 统一替换为减号 '-'，彻底解决 Zig 编译老 glibc 的语法报错
TOOLCHAINS=(
    "$ZIG cc -target x86_64-linux-gnu.2.17"
    "$ZIG cc -target mips64el-linux-gnuabi64.2.17"
    "$ZIG cc -target aarch64-linux-gnu.2.17"
    "$ZIG cc -target loongarch64-linux-gnu.2.36"
)

# ⚡【关键调整】将 pqc-soft-newver 插队放入流程中，刚好在依赖它的 fmssl-protocol-lib 之前
RESOURCE_PATHS=(
    "/home/b/github/fisec/fmcrypto"
    "/home/b/github/fisec/pqc-soft-newver"        # 👈 插队到这里
    "/home/b/github/fisec/fmssl-protocol-lib"
    "/home/b/github/fisec/ssl-vpn-auth-project"
    "/home/b/github/fisec/ssl-pass-server-sdk/sdk"
)

for i in "${!TOOLCHAINS[@]}"; do
    ARCH="${ARCHS[i]}"
    TOOLCHAIN="${TOOLCHAINS[i]}"

    echo "================================================================="
    echo " 🚀 正在启动架构大流水线: [ ${ARCH} ]"
    echo " 🔧 使用工具链: [ ${TOOLCHAIN} ]"
    echo "================================================================="

    for j in "${!RESOURCE_PATHS[@]}"; do
        dir="${RESOURCE_PATHS[j]}"
        cd "$dir" || { echo "进入目录失败: $dir"; exit 1; }
        
        # 判断当前目录是否是 PQC 子工程
        if [[ "$dir" == *"pqc-soft-newver"* ]]; then
            echo ">> [PQC 特殊编译流程] 正在处理: $dir"
            
            # --- 以下融合自原 cross-pqc.sh 的核心逻辑 ---
            XMSS_DIR="xmss"
            PQC_ROOT=$(pwd)

            # 1. 编译依赖库 XMSS
            if [ -d "${XMSS_DIR}" ]; then
                echo "   >> 编译依赖库: xmss"
                cd "${XMSS_DIR}"
                make clean > /dev/null || true
                make CROSS_COMPILE="${TOOLCHAIN}"
                
                XMSS_LIB_PATH="./output/libxmss.a"
                if [ ! -f "${XMSS_LIB_PATH}" ]; then
                    echo "错误: xmss 编译失败，未找到 ${XMSS_LIB_PATH}"
                    exit 1
                fi
                cp "${XMSS_LIB_PATH}" "${PQC_ROOT}/libxmss.a"
                cd "${PQC_ROOT}"
            else
                echo "错误: 找不到 xmss 目录: ${XMSS_DIR}"
                exit 1
            fi

            # 2. 编译 PQC 主工程
            echo "   >> 编译 PQC 主工程"
            make clean > /dev/null || true
            make CROSS_COMPILE="${TOOLCHAIN}"

            # 3. 收集 PQC 产物到它自带的 release_linux
            DIST_DIR="./release_linux/${ARCH}"
            mkdir -p "${DIST_DIR}"
            [ -f "libxmss.a" ] && cp "libxmss.a" "${DIST_DIR}/"
            if ls ./output/*.so 1> /dev/null 2>&1; then cp ./output/*.so "${DIST_DIR}/"; fi
            if ls ./output/*.a 1> /dev/null 2>&1; then cp ./output/*.a "${DIST_DIR}/"; fi
            
            # 清理临时链接文件
            rm -f "${PQC_ROOT}/libxmss.a"

            # 4. 🔥【核心接力桥梁】为了让下一个工程 fmssl-protocol-lib 顺利拿到产物
            # 我们将生成的 .so 文件提取到 PQC 的根目录下，供外层大循环的 cp 逻辑抽取
            if ls ./output/*.so 1> /dev/null 2>&1; then
                cp ./output/*.so ./
            fi
            echo ">> [PQC 流程结束] 产物已就绪"

        else
            # --- 普通工程编译流程（原 cross-compile.sh 逻辑） ---
            echo ">> [标准编译流程] 正在处理: $dir"
            make CROSS_COMPILE="${TOOLCHAIN}" clean || true
            make CROSS_COMPILE="${TOOLCHAIN}"
        fi

        # =================================================================
        # 依赖接力与产物收集（全自动化大循环）
        # =================================================================
        if (( j < ${#RESOURCE_PATHS[@]} - 1 )); then
            next_dir="${RESOURCE_PATHS[j+1]}"
            # 将当前目录下的 .so 传递给下一个待编译的子工程
            cp ./*.so "$next_dir" 2>/dev/null || echo "   ℹ️ 当前目录无 .so 文件，跳过复制接力"
        else
            out="${OUTS[i]}"
            mkdir -p "$out"
            cp ./*.so "$out" 2>/dev/null || echo "   ℹ️ 无 .so 文件，跳过收集"
        fi
    done
    
    # 每一轮架构跑完，把当前目录下可能残存的中间接力 .so 打扫干净
    cd "$PROJECT_ROOT"
done

echo "🎉 降维打击成功！大流水线已一键完成所有依赖编译、跨目录接力及终极剥离！"




objdump -T x.so | grep -oP 'GLIBC_\K[0-9.]+' | sort -Vu

gcc -Wall -Wextra -std=c11 -fsanitize=address -g -o /tmp/rbtree_example rbtree_example.c && /tmp/rbtree_example
$ gcc -Wall -Wextra -std=c11 -I. -fsanitize=address -g -o patricia_example patricia.c patricia_example.c 
gcc -Wall -Wextra -std=c11 -I. -fsanitize=address -g -o patricia_example patricia_copy.c  patricia_copy_example.c 
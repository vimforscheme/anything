### 内存池的必要条件
生命周期高度统一或碎片化严重的需要使用内存池  
延迟要求足够低，内存足够小的时候需要使用内存池
五种内存池描述：
### 1. 一锅端承包区 (Arena Allocator)[nginx]

* **通俗表述：** 像抽纸一样按需撕取，用完后不管三七二十一，连着纸盒整铺扔掉。
* **精确描述：** 基于单向指针偏移的线性连续内存分配器，生命周期结束时整体释放，O(1) 分配且零外部碎片。
* **适用范围：** 生命周期高度统一的短任务（如 Nginx 单个 HTTP 请求、JSON 解析）。

### 2. 标准化格子铺 (Slab Allocator)[ffmpeg]

* **通俗表述：** 像装鸡蛋的模具盒，只准放同样大小的鸡蛋，拿走一个还回一个，严丝合缝。
* **精确描述：** 基于空闲链表（Free List）管理的单一定长对象缓存池，避免系统调用并消除外部碎片。
* **适用范围：** 海量且高频创建/销毁的固定单一数据结构（如 Linux 内核网络包、固定游戏对象）。

### 3. 叠罗汉圆筒 (Stack Allocator)[场景受限]

* **通俗表述：** 东西随便往里堆，但想把底下的抽出来，必须先把压在上面的全拿走。
* **精确描述：** 严格遵循 LIFO（后进先出）规则的线性分配器，伴随函数或任务栈退栈实现无开销的自动回收。
* **适用范围：** 具有严格嵌套关系的局部海量临时计算（如 3D 游戏引擎的每帧物理/渲染矩阵运算）。

### 4. 多尺码储物柜 (Segregated Free Lists)[jemalloc / tcmalloc]{这种通用的多阶内存池，通常会不可避免地白白浪费掉大约 10% 到 20% 的物理内存}

* **通俗表述：** 预先排好几十种尺寸的储物柜，你需要多大空间，就向上取整分给你刚好能塞进的那一种柜子。
* **精确描述：** 维护一组按特定梯度（Size-Class）划分的定长空闲链表数组，以可控的内部碎片为代价换取极高通用性。
* **适用范围：** 对象大小随机、生命周期混沌的通用高并发场景（如 jemalloc/tcmalloc 核心机制、各种语言的底层运行时）。

### 5. 对半劈合体砖 (Buddy System)[系统级别回收]

* **通俗表述：** 嫌大就一直对半劈开给你用，还回来时只要旁边的亲兄弟也空着，就立刻粘合变回原样。
* **精确描述：** 按 2 的幂次方进行等分切割、并在释放时合并物理相邻同级块的内存管理算法，完美保障物理地址连续性。
* **适用范围：** 需要频繁分配和回收各种大小的、且要求物理地址连续的大块内存（如 Linux 内核物理页管理）。

1、2、4最常见
其中因为协议长度不定所以1是7层
2是2-4层常用固定
其中若1搞不定，用4来妥协处理，因为1是比较明确的生命周期较短的做法，太长的话不回收，内存撑不住

### 启示：
比如ssl的处理其实是1和2叠加
就是1起到控制作用2起到数据面的作用
用 Arena 承载“状态”（State），用 Slab 驱动“流量”（Traffic）
1 解决了“杂”的问题： 应对那些乱七八糟、同生共死的配置数据
2 解决了“大”的问题： 应对那些吞吐量极高、频繁进出的业务报文



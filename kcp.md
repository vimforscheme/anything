$$\text{应用层} \longrightarrow \text{KCP 层} \longrightarrow \text{DTLS / DTLCP 层} \longrightarrow \text{UDP 层}$$
dtlcp只保障数据来的内容是合法的就可以，但是他不会给你排序，
哪怕收到协议层的序号3还没收到2但是因为没收到过3所以不是旧包解密校验正常就直接返回给应用层了，这就导致已经乱序了，因为udp无法保序，所以解密正常的包也是应用级别的乱序了，所以才需要kcp在做处理，不直接返回给应用层。
潜伏期
p50 p99 p999
百分比 50% 99% 99.9%的延迟在多少ms以下。
即使99%也不行 因为若中间服务太多每个都是99%没事
那么1-(0.99）^n n越大 整体就越大 也就是被高延迟影响的整体比例就越大，必须避免